# General workflow to build Metal

name: Metal build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        distribution: [ temurin, corretto ]
        java-version: [ 11, 17 ]

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Cache
        uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: ${{ matrix.java-version }}
          distribution: ${{ matrix.distribution }}

      - name: Build with Maven
        run: mvn -B --update-snapshots -Dmaven.test.failure.ignore=true package sonar:sonar -Dsonar.projectKey=parsingdata_metal

      - name: Upload Code Coverage to Codecov
        if: matrix.distribution == 'temurin' && matrix.java-version == '11'
        uses: codecov/codecov-actions@v3
